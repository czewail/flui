import 'package:flutter/material.dart';

class FluuiItemGroup extends StatelessWidget {
  const FluuiItemGroup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
